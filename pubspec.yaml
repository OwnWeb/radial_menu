name: radial_menu
description: A flutter radial menu
version: 0.0.1
homepage: https://github.com/OwnWeb/radial_menu

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  pedantic: ^1.9.0
